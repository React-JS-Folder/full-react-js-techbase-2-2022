CSS MODULES 
Comes with create-react-app 

CSS Modules were created to prevent specificity problems (css classes conflicts)

   To create a module 
   Change css files names to modules 
   example: 
      carousel.scss 
      carousel.module.scss 

      and now
      carousel.module.scss ------ file 
         .header {
            background-color: #000;
         }


      1. import styles from './carouse.module.scss' 
      2. <h3 className={styles.header}> This is heading h3 </h3>

      